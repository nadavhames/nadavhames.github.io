{
  "name": "nadavhames-com",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev.ts",
    "build": "bun run build.tsx",
    "check": "bun run build.tsx --check",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "preact": "^10.29.8",
    "preact-render-to-string": "^6.7.0",
    "prettier": "^3.9.6"
  }
}
